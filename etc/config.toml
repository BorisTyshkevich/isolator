admin = "bvt"

[global]
hosts = [
    "registry.npmjs.org",
    "pypi.org",
    "files.pythonhosted.org",
]

[users.acm]
uid = 600
hosts = [
    "api.anthropic.com",
    "sentry.io",
]

[users.click]
uid = 601
hosts = [
    "api.anthropic.com",
    "api.openai.com",
]

[users.click.auth]
OPENAI_API_KEY = "/etc/isolator/keys/openai"

[users.mcp]
uid = 602
from = "acm"
hosts = [
    "api.anthropic.com",
    "mcp.demo.altinity.cloud",
]
